# Nada aqui e servido como texto. O config, que guarda o token e a
# senha, nunca pode ser lido pelo navegador.
<FilesMatch "^(config|config\.exemplo)\.php$">
  Require all denied
</FilesMatch>
<FilesMatch "\.(md|log|json)$">
  Require all denied
</FilesMatch>
Options -Indexes
